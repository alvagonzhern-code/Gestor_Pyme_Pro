# Contribución

1. Cree una rama desde `main`.
2. Instale con `npm ci`.
3. Ejecute `npm run check` antes de abrir el cambio.
4. Mantenga la lógica fiscal en el servidor y añada pruebas para cualquier cambio en cálculos o estados.
5. No incluya bases de datos, archivos subidos ni secretos en Git.
